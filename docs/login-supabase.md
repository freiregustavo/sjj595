Project URL: https://btcenuztvkaldvpvtiju.supabase.co

As credenciais reais devem ficar apenas no arquivo local `.env`, que nao e versionado.

Variaveis publicas usadas pela aplicacao:

```txt
NEXT_PUBLIC_SUPABASE_URL=https://btcenuztvkaldvpvtiju.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=[PUBLISHABLE-KEY]
```

Direct connection string:

```txt
postgresql://postgres:[YOUR-PASSWORD]@db.btcenuztvkaldvpvtiju.supabase.co:5432/postgres?sslmode=require
```

CLI setup commands:

```bash
supabase login
supabase init
supabase link --project-ref btcenuztvkaldvpvtiju
```
